// Function return

function sumNumbers (a, b) {  
  const result = a + b;
  // Performs a side effect
  console.log(result);
}


function sum (num1, num2) {
  // Return a value from the fuction

  return num1 + num2;
}


const res = sum(5, 10);

console.log('res', res);



// What does a JS function return by default?

// Do JS functions always return a value?